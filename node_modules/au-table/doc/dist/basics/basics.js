"use strict";

System.register(["aurelia-fetch-client"], function (_export, _context) {
    "use strict";

    var HttpClient, BasicsCustomElement;

    function _classCallCheck(instance, Constructor) {
        if (!(instance instanceof Constructor)) {
            throw new TypeError("Cannot call a class as a function");
        }
    }

    return {
        setters: [function (_aureliaFetchClient) {
            HttpClient = _aureliaFetchClient.HttpClient;
        }],
        execute: function () {
            _export("BasicsCustomElement", BasicsCustomElement = function () {
                function BasicsCustomElement() {
                    _classCallCheck(this, BasicsCustomElement);

                    this.open = "${";
                    this.users = [];
                }

                BasicsCustomElement.prototype.bind = function bind() {
                    var _this = this;

                    var client = new HttpClient();

                    return client.fetch('data.json').then(function (response) {
                        return response.json();
                    }).then(function (users) {
                        return _this.users = users.slice(1, 10);
                    });
                };

                return BasicsCustomElement;
            }());

            _export("BasicsCustomElement", BasicsCustomElement);
        }
    };
});
//# sourceMappingURL=basics.js.map
