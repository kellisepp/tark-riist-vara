"use strict";

System.register(["aurelia-fetch-client"], function (_export, _context) {
    "use strict";

    var HttpClient, ApiCustomElement;

    function _classCallCheck(instance, Constructor) {
        if (!(instance instanceof Constructor)) {
            throw new TypeError("Cannot call a class as a function");
        }
    }

    return {
        setters: [function (_aureliaFetchClient) {
            HttpClient = _aureliaFetchClient.HttpClient;
        }],
        execute: function () {
            _export("ApiCustomElement", ApiCustomElement = function () {
                function ApiCustomElement() {
                    _classCallCheck(this, ApiCustomElement);

                    this.open = "${";
                    this.users = [];
                    this.pageSize = 10;
                }

                ApiCustomElement.prototype.bind = function bind() {
                    var _this = this;

                    var client = new HttpClient();

                    return client.fetch('data.json').then(function (response) {
                        return response.json();
                    }).then(function (users) {
                        return _this.users = users;
                    });
                };

                ApiCustomElement.prototype.selectLast = function selectLast() {
                    var last = this.users[this.users.length - 1];
                    last.$isSelected = true;
                    this.tableApi.revealItem(last);
                };

                return ApiCustomElement;
            }());

            _export("ApiCustomElement", ApiCustomElement);
        }
    };
});
//# sourceMappingURL=api.js.map
