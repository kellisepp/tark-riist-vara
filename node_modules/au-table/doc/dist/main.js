'use strict';

System.register(['fetch'], function (_export, _context) {
  "use strict";

  function configure(aurelia) {
    aurelia.use.standardConfiguration().developmentLogging().plugin('aurelia-syntax-highlighter').plugin('aurelia-table').plugin('aurelia-bootstrap');

    aurelia.start().then(function () {
      return aurelia.setRoot();
    });
  }

  _export('configure', configure);

  return {
    setters: [function (_fetch) {}],
    execute: function () {}
  };
});
//# sourceMappingURL=main.js.map
