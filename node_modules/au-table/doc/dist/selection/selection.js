"use strict";

System.register(["aurelia-fetch-client"], function (_export, _context) {
    "use strict";

    var HttpClient, SelectionCustomElement;

    function _classCallCheck(instance, Constructor) {
        if (!(instance instanceof Constructor)) {
            throw new TypeError("Cannot call a class as a function");
        }
    }

    return {
        setters: [function (_aureliaFetchClient) {
            HttpClient = _aureliaFetchClient.HttpClient;
        }],
        execute: function () {
            _export("SelectionCustomElement", SelectionCustomElement = function () {
                function SelectionCustomElement() {
                    _classCallCheck(this, SelectionCustomElement);

                    this.open = "${";
                    this.users = [];
                }

                SelectionCustomElement.prototype.bind = function bind() {
                    var _this = this;

                    var client = new HttpClient();

                    return client.fetch('data.json').then(function (response) {
                        return response.json();
                    }).then(function (users) {
                        return _this.users = users.slice(1, 10);
                    });
                };

                SelectionCustomElement.prototype.rowSelected = function rowSelected($event) {
                    console.log($event.detail.row);
                };

                return SelectionCustomElement;
            }());

            _export("SelectionCustomElement", SelectionCustomElement);
        }
    };
});
//# sourceMappingURL=selection.js.map
