'use strict';

System.register(['aurelia-fetch-client'], function (_export, _context) {
  "use strict";

  var HttpClient, CustomSelectionCustomElement;

  function _classCallCheck(instance, Constructor) {
    if (!(instance instanceof Constructor)) {
      throw new TypeError("Cannot call a class as a function");
    }
  }

  return {
    setters: [function (_aureliaFetchClient) {
      HttpClient = _aureliaFetchClient.HttpClient;
    }],
    execute: function () {
      _export('CustomSelectionCustomElement', CustomSelectionCustomElement = function () {
        function CustomSelectionCustomElement() {
          _classCallCheck(this, CustomSelectionCustomElement);

          this.open = '${';
          this.users = [];
        }

        CustomSelectionCustomElement.prototype.bind = function bind() {
          var _this = this;

          var client = new HttpClient();

          return client.fetch('data.json').then(function (response) {
            return response.json();
          }).then(function (users) {
            return _this.users = users.slice(1, 10);
          });
        };

        CustomSelectionCustomElement.prototype.rowSelected = function rowSelected($event) {
          console.log($event.detail.row);
        };

        return CustomSelectionCustomElement;
      }());

      _export('CustomSelectionCustomElement', CustomSelectionCustomElement);
    }
  };
});
//# sourceMappingURL=custom-selection.js.map
