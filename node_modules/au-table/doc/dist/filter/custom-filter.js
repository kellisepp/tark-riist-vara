"use strict";

System.register(["aurelia-fetch-client"], function (_export, _context) {
    "use strict";

    var HttpClient, CustomFilterCustomElement;

    function _classCallCheck(instance, Constructor) {
        if (!(instance instanceof Constructor)) {
            throw new TypeError("Cannot call a class as a function");
        }
    }

    return {
        setters: [function (_aureliaFetchClient) {
            HttpClient = _aureliaFetchClient.HttpClient;
        }],
        execute: function () {
            _export("CustomFilterCustomElement", CustomFilterCustomElement = function () {
                function CustomFilterCustomElement() {
                    _classCallCheck(this, CustomFilterCustomElement);

                    this.open = "${";
                    this.users = [];
                    this.filters = [{ value: true, custom: this.inactiveFilter }];
                }

                CustomFilterCustomElement.prototype.bind = function bind() {
                    var _this = this;

                    var client = new HttpClient();

                    return client.fetch('data.json').then(function (response) {
                        return response.json();
                    }).then(function (users) {
                        return _this.users = users.slice(1, 10);
                    });
                };

                CustomFilterCustomElement.prototype.inactiveFilter = function inactiveFilter(filterValue, row) {
                    return filterValue || row.isActive;
                };

                return CustomFilterCustomElement;
            }());

            _export("CustomFilterCustomElement", CustomFilterCustomElement);
        }
    };
});
//# sourceMappingURL=custom-filter.js.map
