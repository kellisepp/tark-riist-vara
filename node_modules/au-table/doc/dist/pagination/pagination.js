"use strict";

System.register(["aurelia-fetch-client"], function (_export, _context) {
    "use strict";

    var HttpClient, PaginationCustomElement;

    function _classCallCheck(instance, Constructor) {
        if (!(instance instanceof Constructor)) {
            throw new TypeError("Cannot call a class as a function");
        }
    }

    return {
        setters: [function (_aureliaFetchClient) {
            HttpClient = _aureliaFetchClient.HttpClient;
        }],
        execute: function () {
            _export("PaginationCustomElement", PaginationCustomElement = function () {
                function PaginationCustomElement() {
                    _classCallCheck(this, PaginationCustomElement);

                    this.open = "${";
                    this.selected = 'html';
                    this.users = [];
                    this.pageSize = 5;
                }

                PaginationCustomElement.prototype.bind = function bind() {
                    var _this = this;

                    var client = new HttpClient();

                    return client.fetch('data.json').then(function (response) {
                        return response.json();
                    }).then(function (users) {
                        return _this.users = users;
                    });
                };

                return PaginationCustomElement;
            }());

            _export("PaginationCustomElement", PaginationCustomElement);
        }
    };
});
//# sourceMappingURL=pagination.js.map
